# Generate a long unique value: openssl rand -hex 32
SESSION_SECRET=replace-with-a-long-random-secret
# Generate with: node -e "const c=require('crypto'); console.log(c.scryptSync('choose-a-password','disastermc',64).toString('hex'))"
ADMIN_PASSWORD_HASH=replace-with-a-scrypt-hash
